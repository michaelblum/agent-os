// Canonical M2 proof entrypoint. The owned native projection harness lives in
// the adjacent focused test so it can also run with status-item host changes.
import './operation-native-projections.test.mjs'
