{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/michaelblum/agent-os/shared/schemas/ops-recipe.schema.json",
  "title": "AOS Recipe",
  "description": "Source-backed executable recipe manifest for aos recipe.",
  "type": "object",
  "required": ["id", "version", "summary", "scope", "mutates", "steps"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*(/[a-z][a-z0-9-]*)+$"
    },
    "version": { "type": "integer", "minimum": 1 },
    "summary": { "type": "string", "minLength": 1 },
    "scope": { "enum": ["source"] },
    "mutates": { "type": "boolean" },
    "requires": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "docs": { "type": "string" },
    "resources": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    },
    "parameters": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["default"],
        "additionalProperties": false,
        "properties": {
          "summary": { "type": "string" },
          "default": { "$ref": "#/$defs/JSONValue" }
        }
      }
    },
    "owned_resources": {
      "type": "array",
      "items": { "$ref": "#/$defs/OwnedResource" },
      "uniqueItems": true
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/Step" }
    }
  },
  "$defs": {
    "Step": {
      "type": "object",
      "required": ["id"],
      "additionalProperties": false,
      "anyOf": [
        { "required": ["command"] },
        { "required": ["shell"] },
        { "required": ["kind"] }
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "kind": {
          "enum": ["aos_command", "shell", "recipe_call", "assert", "cleanup", "signal", "gate", "condition", "loop"]
        },
        "command": {
          "type": "object",
          "required": ["path", "form_id"],
          "additionalProperties": false,
          "properties": {
            "path": {
              "type": "array",
              "minItems": 1,
              "items": { "type": "string", "minLength": 1 }
            },
            "form_id": { "type": "string", "minLength": 1 }
          }
        },
        "shell": {
          "type": "object",
          "required": ["script"],
          "additionalProperties": false,
          "properties": {
            "script": {
              "type": "string",
              "pattern": "^[^/].*"
            },
            "cwd": {
              "type": "string",
              "pattern": "^[^/].*"
            }
          }
        },
        "argv": {
          "type": "array",
          "items": { "type": "string" }
        },
        "timeout_ms": { "type": "integer", "minimum": 1 },
        "mutates": { "type": "boolean" },
        "finally": { "type": "boolean" },
        "cleanup_resources": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$"
          },
          "uniqueItems": true
        },
        "assertions": {
          "type": "array",
          "items": { "$ref": "#/$defs/Assertion" }
        }
      }
    },
    "AosCommandStep": {
      "allOf": [
        { "$ref": "#/$defs/Step" },
        {
          "type": "object",
          "required": ["command"],
          "properties": {
            "kind": { "enum": ["aos_command"] }
          },
          "not": { "required": ["shell"] }
        }
      ]
    },
    "ShellStep": {
      "allOf": [
        { "$ref": "#/$defs/Step" },
        {
          "type": "object",
          "required": ["kind", "shell"],
          "properties": {
            "kind": { "const": "shell" }
          },
          "not": { "required": ["command"] }
        }
      ]
    },
    "OwnedResource": {
      "type": "object",
      "required": ["name", "type", "id"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "type": {
          "enum": ["canvas", "runtime", "config", "process", "surface"]
        },
        "id": {
          "type": "string",
          "minLength": 1
        },
        "ttl_seconds": {
          "type": "number",
          "minimum": 0
        }
      }
    },
    "Assertion": {
      "type": "object",
      "additionalProperties": false,
      "oneOf": [
        { "required": ["path"] },
        { "required": ["select"] }
      ],
      "properties": {
        "path": { "$ref": "#/$defs/Path" },
        "select": {
          "type": "object",
          "required": ["path", "where"],
          "additionalProperties": false,
          "properties": {
            "path": { "$ref": "#/$defs/Path" },
            "where": {
              "type": "object",
              "minProperties": 1,
              "additionalProperties": { "$ref": "#/$defs/Scalar" }
            }
          }
        },
        "field": { "$ref": "#/$defs/Path" },
        "exists": { "type": "boolean" },
        "not_exists": { "type": "boolean" },
        "equals": { "$ref": "#/$defs/JSONValue" },
        "contains": { "$ref": "#/$defs/JSONValue" }
      },
      "allOf": [
        {
          "oneOf": [
            { "required": ["exists"] },
            { "required": ["not_exists"] },
            { "required": ["equals"] },
            { "required": ["contains"] }
          ]
        },
        {
          "not": {
            "anyOf": [
              { "required": ["exists", "not_exists"] },
              { "required": ["exists", "equals"] },
              { "required": ["exists", "contains"] },
              { "required": ["not_exists", "equals"] },
              { "required": ["not_exists", "contains"] },
              { "required": ["equals", "contains"] }
            ]
          }
        }
      ]
    },
    "Path": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "Scalar": {
      "type": ["string", "number", "integer", "boolean", "null"]
    },
    "JSONValue": {
      "anyOf": [
        { "type": "string" },
        { "type": "integer" },
        { "type": "number" },
        { "type": "boolean" },
        { "type": "null" },
        { "type": "array", "items": { "$ref": "#/$defs/JSONValue" } },
        { "type": "object", "additionalProperties": { "$ref": "#/$defs/JSONValue" } }
      ]
    }
  }
}
