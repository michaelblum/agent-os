#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd -P)"
cd "$repo_root"

# Compatibility entrypoint. Migrate in-repo work cards/helpers to
# human-needed-runtime-blocker once they can pass the actual runtime verdict.
exec .docks/gdi/scripts/human-needed-runtime-blocker "${1:-accessibility}"
