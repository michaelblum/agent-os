# steward — Codex provider manifest
# Source of truth: ai-agents/agents/steward.md
# Replaces legacy "github-steward" name.

name        = "steward"
description = "GitHub and repo hygiene: branch management, PR lifecycle, issue triage, labels, and release coordination."
nickname_candidates = ["Steward", "GitHub Steward", "Repo Manager"]

[model]
name   = "gpt-5.5"
effort = "low"

[sandbox]
mode = "workspace-write"

[behavior]
github_scope_only = true
