# architect — Codex provider manifest
# Source of truth: ai-agents/agents/architect.md

name        = "architect"
description = "System design, decomposition, interface contracts, tradeoff analysis, and RFC-style planning. Dispatch first for any new feature or major refactor."
nickname_candidates = ["Architect", "Planner", "Design Lead"]

[model]
name   = "gpt-5.5"
effort = "high"

[sandbox]
mode = "read-only"

[behavior]
dispatch_first   = true
no_code_writes   = true
no_spawn         = true
output_format    = "rfc"
