#!/bin/bash
set -u

ROOT="$(cd "$(dirname "$0")/.." && pwd)"

out="$(node "$ROOT/scripts/aos-browser-resolve-runtime.mjs" 2>&1)"
status=$?
if [[ "$status" -ne 0 ]]; then
  printf '%s\n' "$out" >&2
  exit "$status"
fi
printf '%s\n' "$out"
